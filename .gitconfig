[core]
	excludesfile = ~/.gitignore_global
[color]
	ui = true
[merge]
	tool = vimdiff
[push]
	default = current
[mergetool]
	keepBackup = false
[pretty]
        compact = %C(yellow)%h %C(cyan)%an %C(green)%ar%n%x09%C(reset)%s
[format]
        pretty = compact
[alias]
        l = !git --no-pager log master...HEAD
        master = checkout master
        develop = checkout develop
